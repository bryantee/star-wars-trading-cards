{
  "name": "star-wars-trading-cards",
  "version": "1.0.0",
  "description": "grids responsive less design",
  "main": "index.js",
  "scripts": {
    "mkdir": "mkdir -p build",
    "build": "npm run clean && npm run mkdir && npm run build:html && npm run build:css && npm run build:images",
    "clean": "rm -rf build",
    "watch": "npm run watch:html & npm run watch:css",
    "build:css": "npm run build:css:unminified && npm run build:css:minified && npm run build:css:grid",
    "watch:css": "npm run watch:css:unminified && npm run watch:css:minified",
    "clean:css": "npm run clean:css:unminified && npm run clean:css:minified",
    "clean:html": "rm -f build/*.html",
    "build:html": "cp *.html build/index.html",
    "watch:html": "npm run build:html && chokidar *.html -c 'npm run build:html'",
    "clean:images": "rm -fr build/images",
    "build:images": "npm run clean:images && cp -R images/ build/",
    "build:css:grid": "cp css/grid.css build/",
    "build:css:unminified": "npm run clean:css:unminified && lessc --source-map css/main.less build/$npm_package_name.$npm_package_version.css",
    "watch:css:unminified": "npm run build:css:unminified && chokidar 'css/**/*.(css|less)' -c 'npm run build:css:unminified'",
    "clean:css:unminified": "rm -f build/$npm_package_name.$npm_package_version.css build/$npm_package_name.$npm_package_version.css.map",
    "build:css:minified": "npm run clean:css:minified && lessc --clean-css --source-map css/main.less build/$npm_package_name.$npm_package_version.min.css",
    "watch:css:minified": "npm run build:css:minified && chokidar 'css/**/*.(css|less)' -c 'npm run build:css:minified'",
    "clean:css:minified": "rm -f build/$npm_package_name.$npm_package_version.min.css build/$npm_package_name.$npm_package_version.min.css.map",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bryantee/responsive-grid-example-and-challenge-starter-files.git"
  },
  "author": "Bryan Swagerty",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bryantee/responsive-grid-example-and-challenge-starter-files/issues"
  },
  "homepage": "https://github.com/bryantee/responsive-grid-example-and-challenge-starter-files#readme",
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "less": "^2.7.1",
    "less-plugin-clean-css": "^1.5.1"
  }
}
